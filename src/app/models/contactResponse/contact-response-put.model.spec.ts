import { ContactResponsePut } from './contact-response-put.model';

describe('ContactResponsePut', () => {
  it('should create an instance', () => {
    expect(new ContactResponsePut()).toBeTruthy();
  });
});
